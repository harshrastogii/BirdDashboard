# FastAPI platform API dependencies (Phase 3+).
#   pip install -r requirements-api.txt
# The API imports the birddash core, so install requirements.txt too.
-r requirements.txt

fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0
psycopg[binary]==3.2.3
httpx==0.28.1          # test client
