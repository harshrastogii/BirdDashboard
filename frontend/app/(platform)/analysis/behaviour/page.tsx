import { ComingSoon } from "@/components/layout/coming-soon";

export default function Page() {
  return <ComingSoon title="Behaviour Analysis" description="Classify call types — feeding, mating, alarm, territorial, flight — using the call-type labels already captured in the dataset." />;
}
